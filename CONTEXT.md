# MAB Trading Bot — Daily Context Snapshot
Last updated: 2026-05-06 05:00 UTC (06:00 BST) — auto-generated by `mab-context-updater`

> Read this ONE file at the start of any new Cowork session. It captures the previous 24h
> of intel so the brain inheriting the conversation is immediately up-to-speed.

---

## Current Bot State

- **Process status:** RUNNING. Most recent restart at 2026-05-06 05:01:18 UTC; webhook listening on `0.0.0.0:8787/tv`; caffeinate active (Mac will not sleep).
- **Regime:** TRENDING (ADX=27.4, ATR=417.2). Universe scan = 25 symbols/cycle, 45–60 raw → ~24 aggregated signals (top conf cap holding at 9 — slot 10 reserved for AI arbiter, currently offline).
- **Equity (last reading):** $3,163.77 (range over the night $3,091 → $3,170).
- **Open positions:** 1 bot-tracked (`connors_rsi2 ZEC-USDT LONG`, opened 2026-05-06 04:52 UTC, size=1.0, notional≈$52.2, entry 522.17, TP 532.61, SL 514.34, conf=9). Exchange shows 10 positions total — the other 9 are manual/legacy holdings (TAO/ICP/INJ etc.).
- **Today's PnL:** +$1.85 (6 closes — see Strategy Performance below).
- **Lifetime closed PnL:** +$5.60 across 50+ trades.

### Live agents (33 enabled, 8 disabled by backtest)
`whale, news, donchian, candlestick, rsi_divergence, vwap_reversion, trend_pullback, daily_breakout, daily_breakout_24h, daily_breakout_4h, daily_breakout_12h, daily_breakout_48h, daily_breakout_7d, daily_breakout_2h, daily_breakout_8h, supertrend, volume_capitulation, asian_pump, pump_dump_reversal, macd_cross, bb_bounce, zscore_reversion, stoch_rsi, golden_cross, liquidity_sweep, connors_rsi2, raschke_retest, wide_scalp, wide_candle, hurst_regime, kalman_trend, utbot_mtf, utbot_v3, smart_scalp, volume_profile, williams_r, fib_hotzone, funding_extremes, fibonacci`

Disabled (backtest-killed): `scalp, momentum, swing, meanrev, asymmetric, bb_squeeze, funding, ema_ribbon`.

Live notional sizes are 0.04x–0.10x — `connors_rsi2` is the proven workhorse (scaled), `funding_extremes` was promoted small-pilot; everything else is at the standard pilot notional.

### Paper agents
None explicitly tagged paper-only in current config — promotion/kill is handled by enable/disable + backtest-driven `agents disabled` list above.

---

## Last 24h Highlights

1. **State of the Bot v6 published** (`STATE_OF_THE_BOT_2026-05-06.md`) — Saad's "school → PhD" assessment. We are at "end of school / start of high school." Trail-stop widening shipped 04:42 UTC; need 10–20 closes to validate.
2. **Trail-stop widening** is the *only* code change shipped in the last few hours. Per Saad: do not ship more code in the same window so results are attributable.
3. **Backtest verdict from 2026-05-06 runs** (`BACKTEST_2026-05-06.md`, `BACKTEST_TREATISE.md`, `BACKTEST_FIB_TRENDLINES.md`): raw signal edge across 1,935 backtest trades is **NEGATIVE −$293 (29% WR)** — the LIVE 60% WR is being produced almost entirely by the engineered filter pipeline (kill-switch + trend filter + cross-family confluence + pre-position at zone + conf-graded sizing). Honest read: signals are weak; engineering is the edge. Add filters, not agents.
4. **Today's winners:** `connors_rsi2 ZEC-USDT LONG +$1.08 (TP)` at 02:34, `connors_rsi2 DASH-USDT +$0.39` at 03:11, `funding_extremes XMR-USDT short +$0.34 (rec_sync)` at 01:36. Today's losers: `XMR-USDT short −$0.13 (trail)` at 02:19. Net +$1.85.
5. **`hurst_regime` auto-killed by WR-weighted KILL-SWITCH** (WR-weight=0). Roughly 17 signals/cycle now being silently dropped — the system is policing itself correctly.
6. **No NEWS_LOG / SOCIAL_LOG / MACRO_LOG updates in last 24h.** Last NEWS_LOG entry is 2026-05-01 03:42 UTC; SOCIAL_LOG.md and MACRO_LOG.md do not exist. Treat macro context here as *stale by ~5 days* — see Macro section caveat.

---

## Strategy Performance (live, lifetime)

| Agent | Trades | WR | Lifetime PnL | Status |
|---|---|---|---|---|
| connors_rsi2 | 20 | 55% | **+$4.05** ⭐ | Workhorse — keep |
| hurst_regime | 14 | 29% | +$1.12 | KILL-SWITCH dropping signals (WR-weight=0) |
| fibonacci | 4 | 50% | +$0.79 | Live, sample too small |
| tradingview | 1 | 100% | +$0.43 | n=1, noise |
| funding_extremes | 4 | 50% | +$0.31 | Live small-pilot, ok |
| daily_breakout_2h | 2 | 100% | +$0.22 | Highest backtest Sharpe (13.18) |
| daily_breakout_4h | 2 | 50% | −$0.39 | Watch |
| daily_breakout_24h | 3 | 0% | −$0.93 | Worst live; still inside risk envelope |

### Degradation flags
- `hurst_regime`: WR-weight has hit 0 → signals auto-dropped each cycle. Self-policing kill-switch is working. **No intervention required**, but if it stays at 0 for >48h consider undeploy per CEO charter.
- `daily_breakout_24h`: 0% WR / 3 trades is the worst live agent. Still well clear of the −$10 / −$20 cumulative-loss undeploy thresholds. Sample too small to act.
- `connors_rsi2`: Only agent with statistically meaningful sample (n=20). 55% WR + $+4.05 is the real edge in the system. Saad scaled it from 0.05x → 0.10x.

### Backtest leaderboard (`BACKTEST_REPORT.md`, 2026-05-01)
Top 7 PASS strategies: `daily_breakout_4h` (Exp +0.552R, Sharpe 8.94), `daily_breakout_2h` (Sharpe 13.18 — best in cohort), `daily_breakout` (+0.452R), `daily_breakout_24h`, `daily_breakout_12h`, `daily_breakout_8h`, `daily_breakout_48h` (marginal). All currently live.

---

## Macro Context

WARNING — STALE: No macro/news log updates in the last 24h. Last fresh BTC/ETH/SOL pricing in `NEWS_LOG.md` is 2026-05-01 (5 days stale). The bot's internal news fetcher is currently warning on CoinDesk/Cointelegraph/Decrypt RSS (HTTP errors) but successfully tagging 4 symbols/cycle with sentiment.

- **Regime classification (live, internal):** TRENDING. ADX=27.4, ATR=417.2.
- **Last-known prices (2026-05-01 baseline):** BTC +1.63% / ETH +1.37% / SOL +1.09% / S&P near-record. Treat as outdated — re-run a price refresh at session start if a current macro read is needed.
- **Last-known macro flags:** ETF outflows >$490M (CoinDesk), XRP/Fed access talks, broad altcoin pressure expected, funding rates compressing on alts.
- **Known forward catalysts:** Powell exit 2026-05-15; Warsh Senate vote week of 2026-05-11. Both rate-policy events, vol-positive — within the next ~5–10 days from this snapshot.

---

## Pending Actions for Saad

### Deploy Queue (`DEPLOY_QUEUE.md` — 9 strategies awaiting approval)
All from the daily_breakout family + `trend_pullback`. Top picks:
- `daily_breakout_2h` — ExpR +0.466R, Sharpe **13.18** (highest in cohort), n=3,734. Recommended deploy first.
- `daily_breakout_4h` — ExpR +0.501R, Sharpe 10.36, n=2,479.
- `daily_breakout` — ExpR +0.417R, n=290.
- `daily_breakout_24h`, `daily_breakout_12h`, `daily_breakout_8h` — all clean PASS.
- `daily_breakout_48h` — marginal (ExpR exactly +0.201R), 🟡 KEEP rather than 🟢; consider smaller notional.
- `daily_breakout_7d` — re-validate (n=0 in last universe-limited run).
- `trend_pullback` — original PASS; slipped to ExpR +0.153R on fresh universe (now WATCH-tier). 4–8 stronger same-cohort peers exist; consider rejecting in favor of those.

To deploy: reply `deploy <name>` (notional 0.10x).

### Bugs / ops issues (P1, blocking pieces of the system)
1. **AI Arbiter offline** — Claude Max SDK arbiter throws "Command failed with exit code 1" on every high-conviction trade; bot falls back to "approve" by default. Effective AI vetoes are silently disabled. Fix: check Claude SDK install path / OpenRouter fallback. Saad's #1 priority per State of the Bot v6.
2. **`BLOFIN_BROKER_ID` not set** — orders will be rejected by exchange unless added to `~/multi_agent_bot/.env`. Currently fallback handling in place; bot still places trades, but warning spams every reload.
3. **News RSS fetches failing** (CoinDesk/Cointelegraph/Decrypt return HTTP errors on every cycle). Bot still produces "4 symbols with sentiment" via the backup path, but the primary RSS pipeline is degraded.
4. **Telegram egress blocked** — `api.telegram.org` is not on cowork sandbox network allowlist. Every scheduled-task reminder in `DEPLOY_QUEUE_LOG.md` has been blocked at the proxy (curl exit 56, HTTP 403). Today's confirmation message at the end of this synth will fail to deliver too. Fix: add `api.telegram.org` in Settings → Capabilities, OR point scheduled tasks at the bot.py `tg()` helper via an `outbox/` file the bot tails.
5. **Position reconciliation race conditions** — 3 cases of "open in DB but gone from exchange" (DASH/TAO/XMR) self-healed via reconcile_sync this morning. Tighter polling needed (medium priority).
6. **TSLA-USDT, CLANKER-USDT, XPD-USDT** blacklisted for session (HTTP 403 on candles API — symbols not on public candles endpoint).

### Approvals needed
- 9 deploy-queue strategies awaiting `deploy <name>` confirmation (see above).
- All four ops issues above need code/env touches that are outside the autonomous task charter.

---

## Open Questions / Hypotheses

### Strategy ideas under research (`RESEARCH_LOG.md`, last entry 2026-05-01)
- **A1 [TG-NOVEL]** Strategy Decay Detection / Auto-Disable Overlay — rolling per-strategy ExpR + Sharpe with auto-pause threshold. Low-Med implementation. Most actionable open idea.
- A2: Iceberg / refresh-liquidity absorption detection (BloFin tick-data quality dependency).
- A3: HLP-Aware Liquidation Cascade Anticipator (Hyperliquid + Coinglass).
- A4: Path-dependent funding forecast overlay (AR(2) on funding-rate history) — improves `funding_extremes`.
- A5: CEX→DEX lead-lag — N/A (we're on the leading side).

### Coins on hot list awaiting decision (`HOT_COINS.md`, 2026-05-01 sweep)
Trending, NOT endorsements: ACN, PENGU, MEGA, LUNC, ASTEROID, SOL, AI/Gensyn, PROS, HYPE, SKYAI (new), AIGENSYN (new). Small-cap gainers worth watching: **ST (Sentio Token)** — vol/cap 4.71× is the standout. None hit a `trade-now` bar.

### Other open hypotheses
- Trail-stop widening shipped at 04:42 UTC needs the next 10–20 closes to validate. Predicted: avg winner climbs from +0.876% toward +1.4%+.
- Fix the AI arbiter, then add 4H + 1D candles to MarketContext (Fib MTF confluence backtest failed because we faked 4H from 1H), then BTC dominance filter, then real news event blackout, then orderbook imbalance (top-5 bid/ask).

---

## Hard Constraints (DO NOT FORGET)

- **5x leverage cap** — never exceed.
- **$50 daily loss limit** — kill-switch trips at this.
- **5% hard liquidation guard** — equity-percentage circuit-breaker.
- **Manual TAO / ICP / INJ untouched** — these are Saad's manual swing positions; the bot's `our_open` tracker excludes them. Exchange shows ~10 positions today, only 1 is bot-managed.
- **Anthropic safety: Cowork cannot manually fire trades.** This file is read-only context; trades happen only through `bot.py` and webhook flow.
- **CEO charter:** scheduled tasks may not undeploy, deploy, or change parameters autonomously. Synthesis only.

---

## File index (for the next session)

- `STATE_OF_THE_BOT_2026-05-06.md` — Saad's freshest "where we are" report (priority read).
- `BACKTEST_2026-05-06.md`, `BACKTEST_TREATISE.md` — most recent honest backtest reads ("signals weak, engineering is the edge").
- `BACKTEST_REPORT.md` — full strategy verdict table (last full run 2026-05-01).
- `DEPLOY_QUEUE.md` — 9 strategies awaiting Saad's approval.
- `CEO_LOG.md`, `BOARD_MINUTES.md` — running narrative of executive decisions, last entry 2026-05-05 19:01 UTC.
- `RESEARCH_LOG.md`, `HOT_COINS.md` — last sweep 2026-05-01; both are stale.
- `FIBONACCI_TREATISE.md`, `HOT_ZONES_TREATISE.md`, `TREND_LINES_TREATISE.md`, `PERFECT_ENTRIES_RESEARCH.md` — deep theory; only crack open if relevant to a specific question.
- `health.json`, `broker_diag.json` — ops snapshot.
- `bot.log` — 8.3MB; tail last 200 lines for recent state.

---

*Synthesis complete. Telegram confirm attempted (will fail under current proxy block). Saad: this file is the canonical 24h state — start here.*
